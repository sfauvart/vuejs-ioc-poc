import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default function makeStore(apiClient) {
  return new Vuex.Store({
    state: {
    },

    mutations: {
    },

    actions: {
    },

    getters: {
    },
  });
}
